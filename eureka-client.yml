eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/
  instance:
    hostname: lcy
spring:
  application:
    name: client
env:
  dev
label:
  release
